approach_map_runner_node:
  ros__parameters:
    input_cloud_topic: "/livox/lidar" # raw lidar (was "/approach/aligned_cloud" / "/approach/accumulated_cloud")
    target_point_topic: "/approach/target_point"
    grasp_targets_topic: "/approach/grasp_targets"
    mapping_service_name: "/approach/mapping"
    map_frame_id: "map"
    obstacle_map_topic: "/approach/obstacle_map"
    nav2_obstacle_map_topic: "/approach/nav2_obstacle_map"
    feasible_map_topic: "/approach/feasible_map"
    nav2_feasible_map_topic: "/approach/nav2_feasible_map"
    clearance_map_topic: "/approach/clearance_map"
    transition_map_topic: "/approach/transition_count_map"
    robot_filter_frame_id: "base_nav"
    use_initial_origin: false
    allow_origin_updates_after_first_click: false
    mode1_origin_reset_threshold_m: 1.7
    initial_origin_x_m: 0.0
    initial_origin_y_m: 0.0
    ground_z_min_m: -0.30
    ground_z_max_m: -0.10
    obstacle_z_min_m: -0.11
    obstacle_z_max_m: 1.80
    publish_heading_bin: 0
    clearance_display_cap_m: 1.5
    transform_timeout_sec: 0.1
    robot_forced_feasible_radius_m: 0.2
    preprocess_remove_nan_enable: true
    preprocess_downsample_enable: true
    preprocess_outlier_removal_enable: true
    preprocess_voxel_leaf_size: 0.07
    outlier_mean_k: 20
    outlier_stddev_mul_thresh: 1.0
    robot_filter_enable: true
    robot_filter_x_min: -0.30
    robot_filter_x_max: 0.60
    robot_filter_y_min: -0.50
    robot_filter_y_max: 0.50

    # --- Update gating: skip integrating a scan when localization is unreliable ---
    mapping_gate_enable: true
    odom_topic: "/odom"
    odom_frame_id: "odom"
    localization_pose_topic: "/pose"   # AMCL: /amcl_pose, slam_toolbox: /pose (same msg type)
    max_angular_velocity: 0.35              # rad/s
    tf_jump_translation_m: 0.10             # map->odom correction jump -> skip
    tf_jump_yaw_rad: 0.10
    tf_jump_skip_count: 3                   # frames to skip after a jump
    max_position_cov_trace: 0.045           # cov[xx]+cov[yy] (m^2), ~sigma 0.15m/axis
    max_yaw_variance: 0.0036                # cov[yaw] (rad^2), ~sigma 0.06rad
    gate_signal_timeout_sec: 1.0            # ignore odom/pose gate if signal older than this

    # --- Mark the robot trajectory as known-free/feasible ---
    robot_path_feasible_enable: true
    visited_radius_m: 0.25
